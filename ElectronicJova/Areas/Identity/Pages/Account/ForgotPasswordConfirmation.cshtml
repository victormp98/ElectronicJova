@page
@model ForgotPasswordConfirmation
@{
    ViewData["Title"] = "Correo Enviado";
    Layout = null;
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - ElectronicJova</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        body {
            min-height: 100vh;
            background: var(--bg-base);
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Inter', sans-serif;
            color: var(--text-primary);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12 col-md-8 col-lg-5 text-center">
                
                <div class="mb-4">
                    <div class="d-inline-flex align-items-center justify-content-center rounded-circle mb-3 position-relative"
                         style="width: 100px; height: 100px; background: rgba(0, 212, 255, 0.05); border: 2px solid var(--border-accent);">
                        <i class="bi bi-envelope-paper display-4 text-info"></i>
                    </div>
                </div>

                <h2 class="fw-bold mb-3 text-light">¡Revisa tu correo!</h2>
                
                <p class="text-muted mb-4">
                    Si existe una cuenta asociada a ese correo, recibirás instrucciones para restablecer tu contraseña en breve.
                </p>

                <a asp-page="./Login" class="btn btn-outline-light px-4 py-2">
                    <i class="bi bi-box-arrow-in-right me-2"></i>Volver al Login
                </a>
            </div>
        </div>
    </div>
</body>
</html>
