@model PaginatedList<Product>

@{
    ViewData["Title"] = "ElectronicJova — Tecnología de última generación";
}

<div class="row m-0 hero-section mb-5">
    <div class="col-12 text-center position-relative" style="z-index:1;">
        <span class="badge rounded-pill mb-3 px-3 py-2 bg-dark border border-secondary text-info text-uppercase" style="letter-spacing: 0.08em;">
            <i class="bi bi-lightning-charge-fill me-1"></i>Tecnología de última generación
        </span>
        <h1 class="hero-title mb-3">ElectronicJova</h1>
        <p class="hero-subtitle mx-auto">
            Los mejores productos tech a precios increíbles. Envío rápido y garantía total.
        </p>
        <a href="#products" class="btn btn-primary btn-lg rounded-pill px-5 shadow-lg mt-3">
            <i class="bi bi-grid-fill me-2"></i>Ver Productos
        </a>
    </div>
</div>

<div class="container" id="products">
    <div class="row pb-3">
        @foreach (var product in Model)
        {
            <div class="col-6 col-md-4 col-lg-3 mb-4">
                <div class="card h-100 product-card border-0 shadow-sm">
                    <a asp-action="Details" asp-route-id="@product.Id" class="text-decoration-none h-100 d-flex flex-column">
                        <div class="position-relative">
                            <img src="@product.ImageUrl" class="card-img-top" alt="@product.Title" loading="lazy"/>
                            
                            @* Stock Badges *@
                            @if(product.Stock <= 0)
                            {
                                 <span class="badge bg-danger position-absolute top-0 start-0 m-2">Agotado</span>
                            }
                            else if(product.Stock < 5)
                            {
                                 <span class="badge bg-warning text-dark position-absolute top-0 start-0 m-2">¡Últimas unidades!</span>
                            }

                            @* Offer Badge *@
                            @if(product.ListPrice > product.Price100)
                            {
                                <span class="badge bg-success position-absolute top-0 end-0 m-2">
                                    -@(Math.Round((1 - (double)product.Price100 / (double)product.ListPrice) * 100))% OFF
                                </span>
                            }
                        </div>
                        
                        <div class="card-body d-flex flex-column">
                            <h5 class="product-title" style="color: var(--text-primary);">@product.Title</h5>
                            <p class="small text-muted mb-2">@product.Author</p>
                            
                            <div class="price mt-auto product-price">
                                @product.Price100.ToString("c")
                            </div>
                        </div>
                    </a>
                </div>
            </div>
        }
    </div>

    <!-- Pagination Controls -->
    @if (Model.TotalPages > 1)
    {
        <div class="d-flex justify-content-center mt-4">
            <nav aria-label="Page navigation">
                <ul class="pagination">
                    @{
                        var prevDisabled = !Model.HasPreviousPage ? "disabled" : "";
                        var nextDisabled = !Model.HasNextPage ? "disabled" : "";
                    }
                    
                    <li class="page-item @prevDisabled">
                        <a class="page-link rounded-pill px-3 me-2 shadow-sm" asp-action="Index"
                           asp-route-pageNumber="@(Model.PageIndex - 1)"
                           aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span> Previous
                        </a>
                    </li>

                    <li class="page-item active" aria-current="page">
                        <span class="page-link rounded-circle mx-1 shadow-sm">@Model.PageIndex</span>
                    </li>

                    <li class="page-item @nextDisabled">
                        <a class="page-link rounded-pill px-3 ms-2 shadow-sm" asp-action="Index"
                           asp-route-pageNumber="@(Model.PageIndex + 1)"
                           aria-label="Next">
                            Next <span aria-hidden="true">&raquo;</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </div>
    }
</div>


